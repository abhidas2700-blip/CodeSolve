# ThorEye Audit System Environment Variables

# Database Configuration
# For production, use a managed PostgreSQL service like Neon, Supabase, or AWS RDS
DATABASE_URL=postgres://username:password@hostname:port/database?sslmode=require

# Authentication
# Secret key for JWT token generation and session encryption
# In production, use a strong random string (e.g. openssl rand -base64 32)
JWT_SECRET=your-secure-random-string-at-least-32-characters

# Application Settings
NODE_ENV=production

# Optional Settings
# PORT=3000                         # Server port (defaults to 3000)
# LOG_LEVEL=info                   # Logging level (debug, info, warn, error)
# DISABLE_AUTH=false               # Set to 'true' to disable authentication (development only)
# SEED_ADMIN_PASSWORD=admin123     # Initial admin password for seeding (change in production)
