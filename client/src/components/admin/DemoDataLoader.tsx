// DemoDataLoader component has been removed from the production version
// This component was used for generating test data during development

import React from 'react';

export default function DemoDataLoader() {
  return (
    <div className="my-4">
      {/* Demo data loading functionality has been removed from the production version */}
    </div>
  );
}